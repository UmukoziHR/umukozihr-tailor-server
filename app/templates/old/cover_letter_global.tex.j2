% Global Cover Letter - Modern Professional Design
% Professional | Balanced | International Style
\documentclass[11pt,a4paper]{letter}

% === GEOMETRY ===
\usepackage[left=1in, right=1in, top=0.8in, bottom=0.8in]{geometry}

% === FONTS ===
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\renewcommand{\familydefault}{\sfdefault}

% === ICONS ===
\usepackage{fontawesome5}

% === FORMATTING ===
\usepackage{parskip}
\usepackage{xcolor}
\usepackage{hyperref}

% === COLORS ===
\definecolor{headercolor}{RGB}{44, 62, 80}
\definecolor{accentcolor}{RGB}{52, 73, 94}
\definecolor{subtletext}{RGB}{100, 100, 100}

% === HYPERLINKS ===
\hypersetup{
    colorlinks=true,
    linkcolor=accentcolor,
    urlcolor=accentcolor,
    pdfborder={0 0 0}
}

% === SPACING ===
\setlength{\parskip}{10pt}
\setlength{\parindent}{0pt}
\pagenumbering{gobble}

\begin{document}

% === SENDER HEADER ===
\begin{flushleft}
{\large\bfseries\color{headercolor} {{ profile.name }}}\\[4pt]
{% if profile.contacts.email %}\faEnvelope\ {{ profile.contacts.email }}{% endif %}
{% if profile.contacts.phone %} \quad \faPhone\ {{ profile.contacts.phone }}{% endif %}\\
{% if profile.contacts.location %}\faMapMarker*\ {{ profile.contacts.location }}{% endif %}
{% if profile.contacts.links and profile.contacts.links|length > 0 %}\\[2pt]
{% for link in profile.contacts.links[:2] %}
{% if 'linkedin' in link|lower %}\faLinkedin{% elif 'github' in link|lower %}\faGithub{% else %}\faLink{% endif %}\ {{ link }}{% if not loop.last %} \quad {% endif %}
{% endfor %}
{% endif %}
\end{flushleft}

\vspace{8pt}
{\color{headercolor}\rule{\linewidth}{0.5pt}}
\vspace{12pt}

% === DATE ===
\today

\vspace{12pt}

% === RECIPIENT ===
Hiring Manager\\
{{ job.company }}{% if job.location %}\\
{{ job.location }}{% endif %}

\vspace{12pt}

% === SUBJECT ===
\textbf{Re: Application for {{ job.title }}}

\vspace{12pt}

% === GREETING ===
{{ out.address }}

% === INTRODUCTION ===
{{ out.intro }}

% === WHY YOU ===
{{ out.why_you }}

% === EVIDENCE - Key achievements ===
{% if out.evidence and out.evidence|length > 0 %}
Key achievements that demonstrate my qualifications:
\begin{itemize}
\setlength{\itemsep}{4pt}
{% for point in out.evidence %}
\item {{ point }}
{% endfor %}
\end{itemize}
{% endif %}

% === WHY THEM ===
{{ out.why_them }}

% === CLOSING ===
{{ out.close }}

\vspace{16pt}

% === SIGNATURE ===
Sincerely,\\[24pt]
{\bfseries {{ profile.name }}}

\end{document}
